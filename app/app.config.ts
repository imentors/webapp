export default defineAppConfig({
  // Nuxt UI configuration (optional, can be expanded as needed)
  ui: {
    colors: {
      primary: 'teal',
      secondary: 'navy',
      info: 'amber',
      neutral: 'gray',
    }
  }
})
